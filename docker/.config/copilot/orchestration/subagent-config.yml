# Subagent Orchestration Configuration
# Config-first approach for parallel execution patterns
# Reference: docs/retro/workforce/subagent-capabilities.md

metadata: &metadata
  name: "subagent-orchestration"
  description: "Configuration for parallel subagent orchestration patterns"
  version: "2.0.0" # Bumped version for decomposed config
  category: "orchestration"
  updated: "2025-11-26"

<<: *metadata
# NOTE: This configuration is now decomposed into definitions/ directory.
# The loader in orchestrator/config.py will merge files from definitions/
# into this base configuration.

