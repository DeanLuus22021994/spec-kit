# Settings Directory
# Environment-specific and user-specific settings files
#
# This directory is reserved for:
# - Local development settings (not committed)
# - Environment-specific overrides
# - User preferences
# - GPU/CUDA device-specific settings
#
# Usage:
# 1. Copy a template from ../templates/
#    - cp ../templates/settings.template.yml ./local.yml
# 2. Modify ./local.yml with your specific overrides
# 3. The configuration system will load these overrides automatically
#
# Note: All *.yml files in this directory (except .gitkeep) are gitignored.
#
# GPU Configuration Reference:
# - CUDA Version: 13.0
# - PyTorch: 2.9.1+cu130
# - Compute Capability: 8.6
# - See .config/environment.yml for full environment variables

