# Archived Validation Rules

This directory contains validation rules that have been superseded by the centralized validation configuration in `tools/.config/validation/rules/`.

## Archived Content

- **custom-rules/**: Legacy custom validation rules that were duplicated in the new structure.

## Migration

Refer to `tools/.config/validation/rules/ruleset.yaml` for the current active ruleset.
